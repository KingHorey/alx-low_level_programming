#include <stdio.h>
/**
 * main - Start function
 * Description - function prints out numbers from 1 through to 9
 * Return: 0 if successful and 1 if fails
 */

int main(void)
{
	int i = 0;

	for (i = 0; i < 10; i++)
	{
	printf("%d", i);
	}
	printf("\n");
	return (0);
}
