#include "main.h"
/**
 * mul- multiplies integers and returns the result
 * @a: first argument
 * @b: second argument
 * Return: times
 */

int mul(int a, int b)
{
	int times;

	times = a * b;

	return (times);
}

