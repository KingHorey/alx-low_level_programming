/**
 * _strlen- calculates the length of any provided string
 * @s: string to be checked
 * Return: 0
 */

int _strlen(char *s)
{
	int length;

	length = _strlen(s);
	_putchar((length / 10) + '0');
	_putchar((length % 10) + '0');
	return (0);
}
