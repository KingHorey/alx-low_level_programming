#include "main.h"
/**
 * add- add two integers and return the result
 * @x: first argument
 * @y: second argument
 * Return: sums
 */

int add(int x, int y)
{
	int sums;

	sums = x + y;
	return (sums);
}
